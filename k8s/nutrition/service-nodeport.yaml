apiVersion: v1
kind: Service
metadata:
  name: nutrition-service-nodeport
  namespace: nutriplan
spec:
  selector:
    app: nutrition-service
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30001
  type: NodePort
