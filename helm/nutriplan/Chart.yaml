apiVersion: v2
name: nutriplan
description: NutriPlan — мікросервісна система планування харчування
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: postgresql
    version: "15.5.20"
    repository: "https://charts.bitnami.com/bitnami"
    condition: postgresql.enabled
